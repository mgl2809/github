CURSO GITHUB y GITLAB
Comandos de GIT
git init.- Crea un repositorio dentro de la carpeta
ls -a.- mostrar las archivos y carpetas 
vi.- crear editor de texto en consola
git status.- muestra como se encuntran el estatus de los archivos
git add namefile.- para agregar archivos al repositorios
git commit -m "archivo inicial"
vincular GIT PC-Sitioweb
git config --global user.name "mgl2809"
git config --global user.email "marcos_glez"
git config --list .- muestra configuraciones de GIT
git commit -m "message"
git log.- muestra los cambios enviados
estados de GITHUB
workin directory
stating area
repository
crear la llave ssh para protección segura
ir a la carpeta de .ssh consola
ssh-keygen.- crea la llave y se establece una contraseña
git remote add origin https://github.com/mgl2809/github.git 
git remote add origin https://github.com/mgl2809/github.git
git push -u origin master .- para enviar los cambios de local a Github web
git clone y la ruta https://github.com/itebmiranda/test.git.- para clonar el proyecto completo
git pull origin master 
